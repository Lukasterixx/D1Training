# Decision gates

From [the Thesis B plan](../docs/thesis_b_plan.md#gates-and-measurement-definitions). Criteria are
proposed engineering targets until frozen: task-specific values by Week 2, contact calibration by
Week 3. A gate passes only on the evidence it names. Interface smoke runs and training-time
diagnostics do not pass G1.

G4–G7 were added by the plan revision of 16 September, which made a physical AprilTag-guided
combiner-box demonstration the Thesis B deliverable. They are planned work; of the hardware, camera and
perception they describe, only the D1 arm interface has been brought up (2026-09-16, F-020 to F-023).

| Gate | Criterion | Status | Evidence and notes |
| --- | --- | --- | --- |
| G0 · Environment works | Correct articulation/action mapping; finite state; deliberate timeout/fall and partial-reset tests pass; no clone interference; target-frame checks pass | in progress | 2026-09-15: first simulator runs ([Week 1](week_01/notes.md)). `verify` passes 20/20 (F-009): action order and 66/87 widths, 10 Hz arm hold and feedback, leg delays, each termination induced with only that environment resetting, Link6 kinematics and link masses against the CPU model. The arm drive type was fixed along the way (F-010), and a self-collision positive control shows no resting overlap from the weld. The controlled point is now the Link7_1 pincer tip (CAD), matching the model to 0.9 µm (F-013). 2026-09-16: `verify` passes **23/23**, adding two target-box checks — with zero actions the controlled point settles 14.3 cm from the nearest point of the box, and no drawn target is within 5 cm, so no target can be met without moving the arm (F-016). The resting point is measured in the simulator rather than modelled, because the CPU model is 1.6 cm out (F-015). Still open: a visual inspection of posture and target marker (screenshot); clone interference, shown so far only as unaffected control environments. **Re-run needed:** the 23/23 pass was measured with `arm_feedback_period_steps = 5`; the measured 9.00 Hz feedback rate (F-020) moved it to 6 on 2026-09-16 and no GPU was available in that session, so `verify` has not been re-run against the change |
| G1a · Free-space baseline | 100 frozen 10 s episodes per seed: ≥90% reach within 5 cm for ≥1 s continuous; survive to episode end; ≤1% falls | **attempted, failed** | 2026-09-16, three seeds (F-041): seeds 42/43/44 at a matched 73.7M-transition budget, `model_1499` each with no checkpoint selection, on the validation manifest `1fa18142908d`: success **97 / 100 / 97** of 100 — the ≥90% reach criterion is met by every seed — but falls are **3% / 0% / 3%** against ≤1%, so **two of three seeds fail the gate**. All six failures are tilt terminations at 42.0–45.7° against the 45.84° limit, concentrated at far targets (mean depth 0.84 of the box against 0.49 overall; height does not predict them). Seed 43 reaches 39 of 39 of those same far targets at 10.3° median tilt and 0.1% leg saturation, so the task is feasible and this is training variance rather than infeasibility. Development understated it: seed 42 fell once there and three times on validation. Next: a tilt-rate or angular-momentum cost, or a reach-distance curriculum, tuned on `development` only — `validation` is spent for these three policies and `test` is untouched. 2026-09-16, third pass: the squat is fixed and the gate is still not passed (F-040). A `base_height_l2` term at weight −50 against the measured stance removes it outright — **0 of 100** episodes below 0.20 m against 100 of 100, worst margin above `low_base` 6.8 cm rather than 9 mm, steady-state error 30% better at 0.93 cm and leg saturation halved — while the policy reaches with its arm. It scores **99/100** with **1 fall**, and `eval.json` reports `passed: true`, but one fall in 100 *is* 1.0% against a ≤ 1% criterion: on the boundary, Wilson upper bound 5.45%. The failure is a single far-top-corner target reached by pitching to 42.8° against the 45.84° limit; every other episode stays under 13.2°. Still needed: three seeds, a set that is not `development`, and that corner diagnosed. Manifests were re-frozen the same day to carry the robot model itself (F-039), so a changed arm is now a loud mismatch; the zero-action reference is unchanged at 0/100. 2026-09-16, later the same day: the **first trained policy meets the arithmetic and is not a pass** (F-019). `model_1499` from a 73.7M-transition, seed-42 run scores 100/100 on the `development` manifest against 0/100 for zero actions, 0 falls, RMS 2.11 cm. But it reaches by squatting: all 100 episodes drop the base below 0.20 m, median 0.172 m, minimum 0.159 m against the 0.15 m `low_base` termination, with the squat depth correlating +0.50 with target height, and the legs at their effort limit for up to 30% of an episode. One seed, one manifest, and `development` is the inspectable set, so this cannot be the reported result. G1a needs three seeds on a set that is not `development`, and the reward needs a base-height term first. 2026-09-16: the frozen-manifest evaluator is written (`run_position_only.py eval`) and measures the zero-action reference on all three manifests — **0 of 100 episodes** reach on each of development, validation and test, 0 falls, 0 truncated, RMS 21.6-22.2 cm (F-018). `eval.json` checks G1a's own thresholds and currently reports `passed: false`. Manifests are hashed and a run whose conditions differ from the manifest's is flagged. Still needed to attempt this gate: a trained policy, three seeds, and the evaluator exercised against a real failing episode rather than unit tests. The task no longer gives targets away: as of 2026-09-16 the box sits below the resting tool point and zero actions score **0 of 256** within 5 cm, against 12.9% before (F-016, superseding the blocker in F-013). Every target now needs at least 13.5 cm of tool-point motion. The zero-action baseline is still reported beside every reach metric. No policy has been trained against this box: the three Week 1 PPO pilots used the old box, the old spawn and the Link6 point |
| G1b · Moving baseline | G1a criteria on slow trajectories, plus commanded-versus-measured base velocity | not started | Also report gait quality (foot vs neutral point, front–rear spacing, backward after request, turn tracking at several rates, pitch wobble). With the arm on the training robot, Rescue's locomotion policies tracked velocity while gathering their feet (F-008). In playback, yaw is where the welded arm shows first: 31° peak tilt at 1 rad/s against 16° bare (F-012) |
| G2 · Contact suite | Fixture travel/force signs and contact signals calibrated; repeatable initial states; explicit success/failure definitions; no hidden force inputs to P0/P2 | not started | |
| G3 · Experiment ready | ≥3 independent training seeds; matched budgets and test cases; complete configs, checkpoints and metrics saved | not started | Seeded runs reproduce on this machine: pilots 2 and 3 (seed 42) match in all 2,200 learning scalars. They differed only in self-collisions, which the arm never triggered ([Week 1](week_01/notes.md)) |
| G4 · Measured observations and deployment contract | Weeks 2–3: calibrated camera/tag/tool frames; measured timing; real-state FK and matching normalisation and action mapping on both buses; recorded-state replay and disabled-actuation inference agree within frozen tolerances; target transforms stay correct under base motion; a deliberate stale-vision test passes | in progress | Added by the 16 September replan. 2026-09-16: **D1 telemetry measured on the real arm** over DDS from the Go2's Jetson payload — joint angles at 111.0 ms / 9.00 Hz and status at 100.5 ms / 9.96 Hz (F-020), J0 step latency 126–138 ms with peak 1.15 rad/s and steady-state error ≤0.1° (F-021). `motor_model.py` now carries the measured feedback rate. That is one joint, unloaded, on one bus. Still nothing on the camera side: no RealSense, no tags, no calibrated camera/tag/tool frames, no real-state FK, no replay or stale-vision test, and the command rate and J1–J5 response are unmeasured. The arm's own limit table disagrees with its resting pose (F-023), so the zero point must be established before any frame is called calibrated. **Blocker found 2026-09-16:** every servo sign has now been measured against hardware with an observer (F-034) and **two of six are inverted** - J0 (base rotation) and J3 (forearm roll). `deploy.py` maps joints to motor ids by index with **no sign convention**, so `params/deploy.yaml` would mirror both: a policy deployed through it would reach to the wrong side and twist the tool the wrong way. The manifest must carry a per-joint sign before this contract can be frozen. Joint zero offsets are still unmeasured (F-023). The deploy manifest (`params/deploy.yaml`) is written by every run and was checked on paper in Week 1, not against a robot |
| G5 · Physical free-space control | Entry: G4 and candidate G1a, plus G1b for any commanded moving-base case. Exit by Week 4: bounded physical reaching and pose trajectories meet task-specific position/orientation tolerances without falls or limit violations; abort/hold behaviour demonstrated | not started | Added by the 16 September replan. Gated behind G4 (now in progress) and G1a (not started). The exit criterion's "abort/hold behaviour demonstrated" has a **hard** obstacle: the arm ignores DDS power-off (F-022) and release is a torque-off with no holding brake, so an extended arm falls — measured on 2026-09-16 as an uncommanded 60.8° drop at J1 (F-028). **There is no software stop that holds position.** An abort must leave the arm energised and holding, or be planned from a pose where a fall is acceptable |
| G6 · Physical force/contact control | Entry: G2, G5 and successful simulated task cases. Exit by Weeks 4–5: repeatable instrumented low-force contact; force estimate error, peak force and contact loss within predeclared bounds; tool retained and aborts verified | not started | Added by the 16 September replan. No force-aware task exists yet; P0 is position-only |
| G7 · Integrated box demonstration | Entry: G6 and a validated simulated sequence. First complete sequence targeted Week 6; final by Weeks 8–10: ≥16/20 successful physical trials across four frozen box placements × five repeats, no falls or force-limit violations, camera-derived targets driving the learned force-aware tool policy through opening and withdrawal without intervention | not started | Added by the 16 September replan; the Thesis B deliverable |
